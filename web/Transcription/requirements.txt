fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
faster-whisper==1.1.0
# faster-whisper 1.1+ imports `requests` for the model-download path but
# doesn't pin it transitively. Pin a stable wheel range here.
requests>=2.31,<3
numpy==1.26.4
scikit-learn==1.4.2
torch==2.3.1
torchaudio==2.3.1
speechbrain==1.0.2
# speechbrain 1.0.2 still calls `hf_hub_download(..., use_auth_token=...)`,
# which huggingface_hub >= 0.28 removed. Pin below that ceiling so the
# diarization model loads cleanly. Without this, the service starts but
# logs `Speaker diarization unavailable: ... unexpected keyword argument
# 'use_auth_token'` and every transcript falls back to a single speaker.
huggingface_hub>=0.20,<0.28
pydantic==2.7.4
# Phase 4 — multilingual-e5-small embeddings for cahier semantic retrieval.
# Pinned to a version known compatible with huggingface_hub <0.28 (the
# speechbrain pin above). sentence-transformers 2.7.x is the safe band.
sentence-transformers>=2.7,<3

