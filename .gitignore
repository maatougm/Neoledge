# Claude Code local settings (not for repo)
.claude/

# NeoLibrary extracted folder (only keep the .tgz)
deign/components-0.2.123448/

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.user

# Legacy .NET backend (standby, not in active development)
web/Back/

# Node
node_modules/
dist/
*.tsbuildinfo

# Environment / secrets
.env
.env.*
!.env.example
!.env.prod.example

# Build artifacts
*.log
.vite/

# Smoke test output
/tmp/smoke-*.json
testsprite_tests/

# Temp / backup
*.bak
*.tmp

# UX audit screenshots (large binaries, not source)
ux-audit/

# One-off prod smoke scripts (kept locally, not shipped)
web/Front/customapp/prod-*.mjs
web/Front/customapp/smoke-form-audit.mjs
web/Front/customapp/smoke-issues.mjs
web/Front/customapp/smoke-zindex.mjs

# UX audit capture scripts
web/Front/customapp/ux-capture*.mjs

# E2E test outputs — screenshots, CDP dumps, console captures (large binaries / scratch)
scripts/e2e-*-shots/
scripts/cdp-*.json
scripts/console-audit.json
scripts/q-probe.png
scripts/cahier-probe.png
cahier_preview.json

# Codex IDE config
.codex/

# Stray docx in repo root
*.docx

